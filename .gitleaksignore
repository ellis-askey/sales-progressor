# Gitleaks false-positive allowlist.
#
# Each line is a gitleaks fingerprint, format:
#   <commit-sha>:<file-path>:<rule>:<line>
#
# Add a fingerprint here when gitleaks flags real content (not a secret).
# The fingerprint is printed in the pre-push hook output.

# PR 36 (2026-05-02) — "generic-api-key" on a benign content-drafter
# question string ("What's a belief most people in your industry hold
# that you think is wrong?"). No secret material. The commit is already
# on origin/master and has been since the original push.
7b0cd2df3c7ac1bb8bc74e5bc394534f92793521:lib/command/content/voice-questions.ts:generic-api-key:19
