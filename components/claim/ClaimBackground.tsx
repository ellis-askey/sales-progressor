// The claim pages now use a static illustrated street backdrop (set on
// .claim-page in claim-flow.css), so this no longer renders the old animated
// ripples. Kept as a no-op so existing imports stay valid.
export function ClaimBackground() {
  return null;
}
